// This must match the `pluginAlias` in config.schema.json and the `platform` key in the Homebridge config.
export const PLATFORM_NAME = "ELPlatform";

// This must match the `name` in package.json.
export const PLUGIN_NAME = "@ymyzk/homebridge-echonet-lite";
